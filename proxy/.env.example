# Clave de la IA (solo existe en el servidor, nunca llega al navegador)
GEMINI_API_KEY=tu_clave_aqui

# Puerto del servidor proxy (Railway lo asigna automáticamente)
PORT=3001

# Orígenes permitidos (tu dominio de Vercel)
ALLOWED_ORIGIN=https://tu-app.vercel.app
